name: Update BLS Data and Commit

# Run this workflow every Monday at 3 AM UTC
# You can adjust the 'cron' schedule here. '0 3 * * 1' means 3 AM UTC on Monday.
on:
  schedule:
    - cron: '0 3 * * 1'
  # Allows you to manually trigger the data update from the 'Actions' tab on GitHub
  workflow_dispatch:
jobs:
  update_data:
    runs-on: ubuntu-latest

    steps: # <--- Start of the steps block
      # --- Step 1: Checkout ---
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_TOKEN }}

      # --- Step 2: Setup Python ---
      - name: Set up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # --- Step 3: Install Dependencies ---
      - name: Install Dependencies
        run: pip install -r requirements.txt

      # --- Step 4: Run Script (Line 32 is near here) ---
      - name: Run Data Collection Script
        run: python collect_data.py
        env:
          BLS_API_KEY: ${{ secrets.BLS_API_KEY }}

      # --- Step 5: Commit and Push ---
      - name: Commit and Push new data
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "🤖 CI: Updated BLS Data"
          file_pattern: 'data/bls_data.csv'
          add_options: '-f'
          commit_user_name: 'github-actions[bot]'
          commit_user_email: 'github-actions[bot]@users.noreply.github.com'
